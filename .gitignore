# dependencies
node_modules/
.venv/
__pycache__/
*.py[cod]

# art workflow — nothing reaches assets/ without passing verify + human review
staging/

# build output
dist/
.aws-sam/
cdk.out/
*.tsbuildinfo

# bundled Lambda artifacts — infra/build.mjs regenerates these on every deploy
infra/.build/

# local dev-server table (DynamoDB Local's stand-in)
.data/

# local
.env
.env.local
*.local
.DS_Store

# playwright
test-results/
playwright-report/
